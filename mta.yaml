_schema-version: "3.1"
ID: gestion-pme
version: 1.0.0
description: Solution Cloud Gestion Clients & Fournisseurs PME/Startups — SAP BTP
# Region: US10-001 | Org: ffbe6148trial | Space: dev

parameters:
  enable-parallel-deployments: true

build-parameters:
  before-all:
    - builder: custom
      commands:
        - npm ci
        - npx cds build --production
        - powershell -Command "Copy-Item -Path frontend -Destination gen/srv/app -Recurse -Force -Exclude router"

modules:
  # ── Backend CAP Service ──
  - name: gestion-pme-srv
    type: nodejs
    path: gen/srv
    parameters:
      buildpack: nodejs_buildpack
      memory: 256M
      disk-quota: 512M
    requires:
      - name: gestion-pme-db
      - name: gestion-pme-auth
      - name: gestion-pme-dest
      - name: gestion-pme-ias
    provides:
      - name: srv-api
        properties:
          srv-url: ${default-url}

  # ── Database Deployer ──
  - name: gestion-pme-db-deployer
    type: hdb
    path: gen/db
    parameters:
      buildpack: nodejs_buildpack
      memory: 256M
    requires:
      - name: gestion-pme-db

  # ── Approuter ──
  - name: gestion-pme-approuter
    type: approuter.nodejs
    path: frontend/router
    parameters:
      memory: 128M
      disk-quota: 256M
      keep-existing-routes: true
    properties:
      TENANT_HOST_PATTERN: "^(.*).cfapps.us10-001.hana.ondemand.com"
    requires:
      - name: gestion-pme-auth
      - name: gestion-pme-ias
      - name: srv-api
        group: destinations
        properties:
          name: srv-api
          url: ~{srv-url}
          forwardAuthToken: true

  # ── SAP Build Apps Destination Content ──
  - name: gestion-pme-dest-content
    type: com.sap.application.content
    requires:
      - name: gestion-pme-dest
        parameters:
          content-target: true
      - name: gestion-pme-auth
        parameters:
          service-key:
            name: gestion-pme-auth-key
      - name: srv-api
    parameters:
      content:
        instance:
          destinations:
            - Name: gestion-pme-api
              Description: Backend API for SAP Build Apps
              Authentication: OAuth2UserTokenExchange
              TokenServiceInstanceName: gestion-pme-auth
              TokenServiceKeyName: gestion-pme-auth-key
              URL: ~{srv-api/srv-url}
              sap.cloud.service: gestion-pme
              sap.applicationdevelopment.actions.enabled: true
              sap.build.usage: odata_gen
              HTML5.DynamicDestination: true
              WebIDEEnabled: true
              WebIDEUsage: odata_gen
          existing_destinations_policy: update
    build-parameters:
      no-source: true

resources:
  # ── Réutilise l'instance HANA existante ──
  - name: gestion-pme-db
    type: org.cloudfoundry.existing-service
    parameters:
      service-name: my-hana-db

  # ── Nouvelle instance XSUAA dédiée avec nos rôles ──
  - name: gestion-pme-auth
    type: org.cloudfoundry.managed-service
    parameters:
      service: xsuaa
      service-plan: application
      path: ./backend/xs-security.json
      config:
        xsappname: gestion-pme
        tenant-mode: dedicated

  # ── Réutilise le Destination Service existant ──
  - name: gestion-pme-dest
    type: org.cloudfoundry.existing-service
    parameters:
      service-name: Destination-Service

  # ── Réutilise l'HTML5 App Repository existant ──
  - name: gestion-pme-html5-repo-rt
    type: org.cloudfoundry.existing-service
    parameters:
      service-name: HTML5-APP

  # ── Cloud Identity Services (IAS) ──
  - name: gestion-pme-ias
    type: org.cloudfoundry.managed-service
    parameters:
      service: identity
      service-plan: application
